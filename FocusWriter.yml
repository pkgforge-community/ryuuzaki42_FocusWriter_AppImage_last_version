app: focuswriter

ingredients:
  dist: focal
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ focal main universe
  ppas:
    - gottcode/gcppa

script:
  #- sudo apt-get update
  #- sudo apt-get install focuswriter
  - cp ../../README.md .
