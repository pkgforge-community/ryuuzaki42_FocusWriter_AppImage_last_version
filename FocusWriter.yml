app: focuswriter

ingredients:
  dist: focal
  packages:
    - focuswriter=1.8.6
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ focal main universe

script:
  - set -x
  - ls -lhaR
  - cp ../../README.md .
